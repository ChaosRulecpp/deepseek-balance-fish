# 桌宠软件需求文档(交给 pro 模型实现)

项目根目录:`C:\Users\22896\Desktop\ds fish`。开发环境:Windows 11、Git Bash、Node.js v24。

## 1. 技术栈
- **Electron + 纯 JS**(不引前端框架,PNG 帧序列动画用 `<canvas>` 绘制即可)。
- 窗口要求:无边框、背景透明(点击透明像素穿透)、置顶、不在任务栏显示(用系统托盘图标,托盘菜单提供「退出」「显示余额」等)。
- 支持拖拽移动桌宠,退出时记住窗口位置。

## 2. 美术资产(已就绪,直接使用)
| 用途 | 路径 | 帧数 | 尺寸 | 帧率 |
|---|---|---|---|---|
| 工作(循环) | `assets/transparent/work/` | 97 | 720×720 | 24fps(≈4s) |
| 睡眠(循环) | `assets/transparent/sleep/` | 97 | 720×720 | 24fps |
| 工作→睡眠过渡 | `assets/transparent/work to sleep/` | 97 | 720×720 | 24fps |
| 睡眠→工作过渡 | `assets/transparent/sleep to work/` | 97 | 720×720 | 24fps |
| token 旋转(循环) | `assets/transparent/token/flip/frame_0001.png ~ frame_0024.png` | 24 | 512×512 | 建议 24fps |

文件名格式:`frame_%04d.png`(如 `frame_0001.png`)。动画播放到最后一帧后回到第 1 帧循环。

## 3. 状态机

状态:`WORK`(工作)、`SLEEP`(睡眠)、`TRANSITION`(过渡动画播放中,目标状态记录在内部)。

**定时检查**:每 **1 分钟** 调一次余额接口。
- 首次查询只记录基线,不触发状态变化。
- 从第二次查询起,与上一次余额比较:
  - **余额没变 或 余额增加(充值)**:当前是 `WORK` → 播「工作→睡眠」过渡 → 进入 `SLEEP`;当前是 `SLEEP` → 保持 `SLEEP`。
  - **余额减少**:当前是 `SLEEP` → 播「睡眠→工作」过渡 → 进入 `WORK`;当前是 `WORK` → 保持 `WORK`。
- 查询失败(网络错误 / 401 / 超时):**不改变状态**,保持当前状态,下次定时再查;可把失败次数累计,连续失败给出提示(气泡「余额查询失败」)。

**点击桌宠**:
1. 先做**实体像素命中检测**:取当前显示帧在该像素的 alpha,`alpha > 0` 才算点到(透明区域不触发)。
2. 触发后播放 **token 旋转下落动画**:程序控制下落轨迹(建议带重力/缓动,从桌宠上方落到下方),下落过程中循环播放 `token/flip` 的 24 帧实现旋转,下落结束后淡出。
3. 同时**立即查询一次余额**(并刷新悬停气泡里的余额显示)。
4. 无论之前什么状态,点击后**强制唤醒**:`SLEEP` → 播「睡眠→工作」→ `WORK`;`WORK` → 保持 `WORK`。
5. 过渡动画播放期间点击:允许打断,以点击逻辑为准。

## 4. 余额接口
- 地址:`GET https://api.deepseek.com/user/balance`
- 请求头:`Authorization: Bearer <API_KEY>`
- 响应 JSON 结构(取第一项的 `total_balance` 作为展示余额):
  ```json
  {
    "is_available": true,
    "balance_infos": [
      { "currency": "CNY", "total_balance": "12.34", "granted_balance": "0", "topped_up_balance": "12.34" }
    ]
  }
  ```
- 该接口是普通 HTTP GET,**不消耗任何 token、不产生费用**,频率 1 分钟一次安全。
- **API Key 存放**:项目根目录 `config.json`(字段 `apiKey`、`checkIntervalMs=60000`、`windowPosition`),`.gitignore` 排除。首次运行若没配置,弹窗让用户粘贴 API Key 并保存。不要把 key 硬编码进代码。

## 5. 余额显示
- 鼠标**悬停**在桌宠上时,弹一个半透明气泡显示当前余额(`total_balance` 保留 2 位小数,如 `¥12.34`),移开消失。
- 点击桌宠触发查询后,同步刷新该气泡数值。

## 6. 启动行为
- 启动时**先查一次余额**记录基线,然后直接进入 `WORK` 状态(播工作动画)。

## 7. 其他约定
- 动画循环播放帧率:按 24fps 播放,与原始素材一致。
- 窗口默认尺寸按素材比例(720×720),支持运行时缩放(建议保留像素图缩放后仍清晰——用 `imageSmoothingEnabled` 控制,或固定尺寸不缩放)。
- 托盘菜单:`显示余额`、`退出`。
- 建议代码结构:`main.js`(主进程:窗口、托盘、定时器、余额请求)、`renderer`(渲染动画、命中检测、交互)、`config.json`(配置)。
